apiVersion: apps/v1
kind: StatefulSet
metadata:  
  name: frontend
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  serviceName: frontend  
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:     
      - name: predictionservice
        image: mar3ek/ech-hh-predictionservice:latest        
        ports:
        - containerPort: 80
          name: api
          protocol: TCP
      